import 'package:flutter/material.dart';
import 'package:myflutterject/screen/item.dart';

void main() {
  
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
        title: "Flutter",
        home: Scaffold(
          appBar: AppBar(
            title: const Text("หน้าเเรกเเอพ"),
            backgroundColor: Colors.blue,
            centerTitle: true,
          ),
          body: const Item(),
        ));
  }
}
class  extends StatelessWidget {
  const ({super.key});

  @override
  Widget build(BuildContext context) {
    retu
  }
}

